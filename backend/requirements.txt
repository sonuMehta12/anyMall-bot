# Phase 0 — MVP dependencies
# Phase 1C — added database packages

fastapi>=0.110.0          # Web framework — routing, request/response handling
uvicorn[standard]>=0.29.0 # ASGI server that runs FastAPI. [standard] adds performance extras
pydantic>=2.0.0           # Data validation — FastAPI uses this for all request/response models
pydantic-settings>=2.0.0  # Extends Pydantic to read config from .env files
openai>=1.30.0            # Azure OpenAI + direct OpenAI SDK (same package, handles both)
python-dotenv>=1.0.0      # Loads .env file into os.environ on startup

# ── Database (Phase 1C) ─────────────────────────────────────────────────────
sqlalchemy[asyncio]>=2.0.0  # ORM + async engine for PostgreSQL
asyncpg>=0.29.0             # Fastest async PostgreSQL driver for Python
alembic>=1.13.0             # Database schema migrations (versioned DDL changes)

# ── AALDA API integration ──────────────────────────────────────────────────
httpx>=0.27.0               # Async HTTP client for AALDA API calls

# ── Valkey cache (ft-005) ───────────────────────────────────────────────────
valkey[hiredis]>=6.0.0      # Valkey client (Redis-compatible, BSD licence).
                             # [hiredis] adds a C-based response parser for 2-10x
                             # faster parsing at zero code cost.

# ── Nightly scheduler (Sprint 6) ────────────────────────────────────────────
apscheduler>=3.10.0          # AsyncIOScheduler runs jobs inside the asyncio event loop.
                             # Midnight UTC cron for HistoryBuilder + RelationshipBuilder.

# ── Recipe MCP (Phase 3 — Recipe Integration) ──────────────────────────────
mcp>=1.0.0                  # MCP Python SDK — Streamable HTTP client for Recipe MCP server

# ── Tavily Web Search (Food AI — Phase 4) ──────────────────────────────────
tavily-python>=0.5.0        # Tavily async search client — used by FoodAgent for web context
